-- ============================================================
-- Phase 6 — مصادر العملاء (Lead Source)
-- شغّله في SQL Editor بعد باقي ملفات schema
-- إضافة فقط، آمن على البيانات الموجودة
-- ============================================================

alter table clients add column if not exists lead_source text
  check (lead_source in ('إحالة عميل', 'معرض / فعالية', 'اتصال بارد', 'موقع الشركة', 'سوشيال ميديا', 'أخرى'));
