-- ============================================================
-- Phase 4 (مُعدَّلة) — تتبّع مرحلة التنفيذ بعد كسب الصفقة
-- شغّله في SQL Editor بعد schema.sql, schema_v2.sql, schema_v3.sql
-- إضافة فقط، آمن على البيانات الموجودة
-- ============================================================

alter table clients add column if not exists implementation_stage text
  check (implementation_stage in (
    'العقد موقّع',
    'تركيب الهاردوير',
    'إدخال البيانات',
    'التدريب',
    'اختبار تجريبي',
    'Go-Live فعلي'
  ));

-- ملاحظة: لو كنت شغّلت schema_v4.sql القديم (بتاع تذاكر الدعم) قبل كده،
-- جدول support_tickets هيفضل موجود من غير ما يستخدمه حد — مش ضروري تمسحه،
-- بس لو حابب تنضّف قاعدة البيانات، شغّل السطر ده لوحده:
-- drop table if exists support_tickets;
